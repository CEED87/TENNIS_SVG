const racketLeft = document.querySelector('.racket');
const racketRight = racketLeft.nextElementSibling;
const circle =document.querySelector('circle');



document.addEventListener('keydown', (e) => {
    if (e.keyCode === 16) {

        console.log(e.keyCode)
    }
    if (e.keyCode === 17) {
        console.log(e.keyCode)
    }

    
});